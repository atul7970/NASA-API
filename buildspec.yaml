version: 0.2

phases:
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run deploy
  post_build:
    commands:
      - echo Build completed on `date`

artifacts:
  files:
    - '**/*'
  base-directory: server/build
  discard-paths: no
